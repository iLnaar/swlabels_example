
name: swlabels_example
description: SWLabels example (Flutter)
version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  spritewidget:
  swlabels:
    path: '../swlabels'

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
